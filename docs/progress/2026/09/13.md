# Progress Log - 2026-09-13

## Incident

- Backfill legacy `CalendarIncident` / `CalendarIncidentChronology` rows to `LIVE` inside migrations `0015` / `0016` (uncommitted)
- Added reversible `RunPython` data migration so the `DRAFT` model default only applies to rows created after the migration
- Added `tests/incident/test_legacy_status_backfill.py` (3 tests); full suite verified on a fresh test DB
