# 2026-09-24

## Telegram: `/spotting_today` referenced a non-existent `VehicleStatus` member

- **What**: `telegram_provider/utils.py::get_daily_updates` excluded
  `vehicle__status=VehicleStatus.NOT_IN_SERVICE`, an attribute that does not exist on
  `operation.VehicleStatus` (the member there is `OUT_OF_SERVICE`; only
  `spotting.SpottingVehicleStatus` keeps a `NOT_IN_SERVICE` member). The exclusion now uses
  `VehicleStatus.OUT_OF_SERVICE`, so the default `/spotting_today` digest and the 03:00
  `spotting.tasks.report_spotting_today` push render again.
- **Why**: `/spotting_today` in the TranSPOT Test channel raised
  `AttributeError: type object 'VehicleStatus' has no attribute 'NOT_IN_SERVICE'` at
  `utils.py:238`; the flag (and this reference) landed in `0882c80` and never ran against a
  real out-of-service roster in tests.
- **Tests**: `telegram_provider/tests.py::GetDailyUpdatesTests` (real ORM, no mocks) — the
  default digest omits an `OUT_OF_SERVICE` vehicle, `include_not_in_service=True` lists it.
  `ruff check`/`ruff format` clean; `manage.py check` clean; 55-test `telegram_provider`
  suite → 50 pass (5 pre-existing `LinkHandlerTests` Firebase-env errors, confirmed present
  with the change stashed).
- **Commit**: 6075118.

## Telegram: `/spotting_today` service-day cutoff (3am default)

- **What**: `/spotting_today` now resolves "today" through a configurable day cutoff:
  anything before 03:00 (in `settings.TIME_ZONE`) counts as the previous calendar day, so a
  command run at 02:59 on 24 Sep reports 23 Sep. New flags: `--use-actual-date` (12am
  cutoff) and `--cutoff=HHMM` (24-hour custom cutoff, e.g. `--cutoff=0430`); the two are
  mutually exclusive, malformed values reply with a usage error, and an explicit positional
  date is never shifted. `utils.effective_spotting_date(now, cutoff)` is the pure helper;
  `get_daily_updates` and every other caller are untouched (its rendered header still uses
  `date.today()`, deliberately out of scope).
- **Tests**: `EffectiveSpottingDateTests` (boundary, aware/naive, month/year rollover),
  `SpottingTodayHandlerTests` (cutoff selection, timezone of `now`, explicit-date
  precedence, invalid cutoff, mutual exclusion), `LinkHandlerTests` parser cases (HHMM
  parsing, malformed rejection, exclusivity). 17/17 targeted tests pass; 74-test
  `telegram_provider` suite → 69 pass (same 5 pre-existing `LinkHandlerTests`
  Firebase-env errors, confirmed on the stashed baseline). `ruff check`/`ruff format`
  clean; `manage.py check` clean.
- **Commit**: 42d8b12.

## Operation: Line admin loaded every CalendarIncident (slow change form)

- **What**: `operation/admin.py::LineAdmin` now declares
  `exclude = ("calendar_incidents",)`. Without it Django auto-rendered the
  `Line.calendar_incidents` M2M as a `<select multiple>` that pulled every
  `CalendarIncident` row whenever a Line change page opened; excluding the field also
  drops its default `blank=False` required validation, so saving a Line can no longer be
  blocked by an empty incident selection. The relation stays editable from
  `CalendarIncidentAdmin` (`filter_horizontal = ("lines", …)`).
- **Why**: Loading `/admin/operation/line/<id>/change/` was very slow; the incident
  multi-select was the dominant query.
- **Tests**: `operation/tests.py::LineAdminConfigTests` asserts
  `calendar_incidents` is absent from the Line change-form fields. `ruff check`/`ruff
  format` clean; `manage.py check` clean; `manage.py test operation --keepdb` → 24 pass.

## Python: upgrade runtime to 3.13

- **What**: base image `python:3.12-slim-bookworm` -> `python:3.13-slim-bookworm`;
  `requires-python >= 3.12` -> `>= 3.13`; `.python-version` (gitignored, local-only) ->
  `3.13`; new `rosak/test_python_runtime.py` guard tests assert the running interpreter is
  >= 3.13 and that the Dockerfile/pyproject pins stay aligned (`.python-version` asserted
  only when present, because it is gitignored and absent in CI checkouts).
- **Why**: runtime upgrade; the guard test makes a silent downgrade fail the standard gate.
- **Tests**: pre-upgrade 3.12.14 baseline was 285 tests / 2 failures / 5 errors;
  post-upgrade 3.13.15 gives 287 tests / 2 failures / 5 errors with the **same 7
  pre-existing failure IDs** (5 `telegram_provider.tests.LinkHandlerTests`
  `ValueError: Invalid uid: MagicMock`, 2 `incident.tests.SocialMediaLinkTests`
  `No user record found ... test-user-sml`); guard tests 2/2 pass; `manage.py check` and
  `makemigrations --check --dry-run` clean; `ruff check`/`ruff format --check` clean on
  changed files; native imports (psycopg2, polars, uvloop, granian, pillow-heif,
  cryptography, hiredis, psutil, zmq, watchfiles) all OK on cp313; GraphQL `POST
  /graphql/` through nginx returns HTTP 200. Note that `pendulum==3.0.0` has no cp313
  wheel and is built from sdist via maturin (auto-provisioned Rust) during image build;
  and that `python -X dev manage.py check` reports 34 pre-existing upstream
  DeprecationWarnings (strawberry_django 27, django-health-check 6, debug-toolbar 1), 0
  ResourceWarnings.
- **Commit**: bc45bf1.

## Compose: celerybeat lost the firebase credential mount

- **What**: `docker-compose.yml` `celerybeat` now mounts
  `${HOME}/.firebase/rosak-7223b-firebase-adminsdk-8hcki-f2e0ee7994.json:/google-application-credential.json`,
  mirroring the `app` service.
- **Why**: the service's explicit `volumes:` list replaces the `*app` anchor's list (YAML
  merge does not deep-merge sequences), so celerybeat never received the credential bind;
  on container recreation it crash-looped with `ImproperlyConfigured` from
  `spotting.apps.SpottingConfig.ready()`. Pre-existing config defect, exposed when the
  stack was recreated for the 3.13 upgrade.
- **Tests**: `docker compose config -q` clean; celerybeat stays `Up` and dispatches all six
  periodic tasks (`cleanup_temporary_media`, `aggregate_line_vehicle_status_mlptf`,
  `cleanup_expired_verification_codes`, `cleanup_telegram_logs`,
  `purge_soft_deleted_incidents`, `purge_rejected_incidents`).
- **Commit**: 2acbbc2.

## Deps: refresh pinned dependencies (pendulum 3.2 + 15 same-major bumps)

- **What**: 16 direct bumps: `pendulum` `3.0.0` -> `3.2.0`; `anyio` `4.15.1`,
  `boto3` `1.43.101`, `django-admin-rangefilter` `0.15.0`, `django-cachalot` `2.9.1`,
  `djangorestframework` `3.18.1`, `firebase-admin` `7.7.0`, `granian` `2.8.3`,
  `httpcore` `1.0.9`, `pillow-heif` `1.8.0`, `polars` `1.44.2`, `psutil` `7.2.2`,
  `psycopg2-binary` `2.9.13`, `pygments` `2.21.0`, `sentry-sdk` `2.70.0`,
  `watchfiles` `1.3.0`. Transitive: `botocore` `1.43.101`, `h11` `0.16.0`,
  `polars-runtime-32` `1.44.2`; removals `sniffio`, `time-machine`. `pyproject.toml`
  pendulum floor tightened to `>=3.2.0`. `redis` intentionally left at `5.2.1` (major
  bump; `DEBUG=True` -> `DummyCache` means cache paths are untested locally).
- **Why**: `pendulum==3.0.0` has no cp313 wheel and required a Rust/maturin sdist
  build in every image build; the rest are same-major refreshes inside declared
  constraints.
- **Tests**: images rebuilt; all 16 versions verified installed; `pip show sniffio
  time-machine` -> not found; build log shows no maturin/pendulum source build;
  `manage.py check` + `makemigrations --check` clean; 287 tests, failures=2/errors=5
  with the identical 7 pre-existing IDs as the pre-bump baseline; guard tests 2 + 11
  pass; imports OK; GraphQL `POST /graphql/` HTTP 200; celerybeat dispatching due
  tasks. Note: `pendulum.__version__` emits a deprecation warning on 3.2
  (informational).
- **Commit**: fc72187.

## Test: fix the 7 pre-existing failures (Firebase admin check mocked)

- **What**: test-only. `LinkHandlerTests._mock_handlers` patches
  `rosak.permissions.has_admin_claim` (correct target because
  `telegram_provider.handlers.submit_link` imports it function-locally), mirroring the
  existing `MediaUploadTests` pattern; `test_submit_link_with_incident_id_calls_service_once`
  additionally asserts `is_admin` flows through. The two `SocialMediaLinkTests` submit tests
  patch `incident.schema.mutations.interactions.has_admin_claim` (correct target because that
  resolver imports it at module level and calls it directly — unlike the delete tests, which
  patch `rosak.permissions` because the `IsAdmin` permission class reads its own module
  global).
- **Why**: both groups called the live Firebase Admin API — Group A with a `MagicMock` uid
  (`ValueError: Invalid uid`), Group B with a DB-only user (`UserNotFoundError: No user record
  found for the provided user ID: test-user-sml`). These 7 failures pre-dated the Python 3.13
  upgrade and were recorded as pre-existing in the entries above.
- **Tests**: full suite `Found 287 / Ran 287 / OK` (previously 2 failures + 5 errors);
  targeted `LinkHandlerTests` + `SocialMediaLinkTests` 30 tests OK; `ruff check` + `ruff format
  --check` clean on both files.
- **Commit**: 51f2f69.

## Rosak: has_admin_claim no longer 500s when the Firebase account is missing

- **What**: `rosak/permissions.py::has_admin_claim` now wraps the `sync_to_async(auth.get_user)`
  lookup in `try/except auth.UserNotFoundError` and returns `False` (deny) instead of raising;
  the `if not user: return False` short-circuit is unchanged. New test file
  `rosak/tests/test_permissions.py` (6 tests).
- **Why**: a user whose Firebase account was deleted upstream while the `common.User` row
  remained made `UserNotFoundError` propagate out of `IsAdmin` and the 12 conditional-admin
  resolvers in `incident/schema/mutations/` — a GraphQL 500 rather than a permission denial.
- **Tests**: `rosak/tests/test_permissions.py` covers the None short-circuit (and asserts
  `get_user` is never called), `custom_claims={"admin": True}` -> True, `{}` -> False, `None` ->
  False, `UserNotFoundError` -> False, and a non-`UserNotFoundError` `FirebaseError` propagating
  (locks the catch narrow). Full suite `Found 293 / Ran 293 / OK`; `ruff check` + `ruff format
  --check` clean on both files. Note: `firebase_admin.auth` does not re-export `FirebaseError`,
  so the test imports it from `firebase_admin.exceptions`.
- **Commit**: 6a8f25e.
