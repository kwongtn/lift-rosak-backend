# 2026-09-23

## Incident: `statusCounts` docs — name the reused type

- **What**: `docs/components/incident.md` now states the origin of the hourly bucket's
  `statusCounts` field — it is `operation.schema.scalars.PassengerStatusCount`, reused rather than an
  incident-local parallel type. Docs only; no code, test, or schema change.
- **Why**: the feature itself (the `HourBucket.status_counts` pass-through, the resolver, the schema
  snapshot) and its full progress entry landed in `22baa99` and are recorded in
  `docs/progress/2026/09/22.md`. That entry named the reuse, but the component doc named the type
  without saying where it lives, so a reader could not tell whether the breakdown introduced a second
  type or borrowed the per-line one.
- **Commit**: (this commit).
