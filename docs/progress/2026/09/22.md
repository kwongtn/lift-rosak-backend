# 2026-09-22

## Incident: PassengerStatus, LineStatusReport, and canonicalized social-link URLs

- **What**: Added the `PassengerStatus` `TextChoices` enum (`NORMAL`, `BUSY`, `CROWDED`,
  `EXTREMELY_CROWDED`, `BACKLOGGED`, `DELAYED`, `DISRUPTED`) and a new `incident.LineStatusReport`
  model — `line` FK→`operation.Line` (`PROTECT`, `related_name="status_reports"`), `stations` M2M,
  `status` `TextChoicesField`, nullable `delay_minutes`, `notes`, `user` FK→`common.User`, and an
  optional `link` FK→`SocialMediaLink` (`SET_NULL`, `related_name="status_reports"`;
  `Meta.ordering=["-created"]`). `SocialMediaLink` gained `normalized_url` (plain indexed
  `CharField`, **not** unique) and a `votes` `GenericRelation`.
- **Why**: The community front page needs a crowd-reported passenger-load signal per line, and a
  stable identity for a shared social post that ignores tracking parameters.
- **How**:
  - `incident/enums.py` — `PassengerStatus` (`f3fb273`).
  - `incident/models.py` — `LineStatusReport`; `SocialMediaLink.normalized_url` recomputed on every
    `save()` (`f3fb273`).
  - `incident/services/urls.py` — `canonicalize_url`: strips tracking params (`fbclid`, `gclid`,
    `igshid`, `si`, `utm_*`, …), lowercases the host, drops the fragment and trailing slash, keeps
    non-default ports; non-http(s) or unparseable input is returned unchanged (`e0884ae`).
  - `incident/migrations/0024_socialmedialink_normalized_url_linestatusreport.py` — adds the column,
    backfills it in 500-row batches (reversible `RunPython`), then creates `LineStatusReport`.
- **Deliberate deviation**: `normalized_url` carries **no** DB unique constraint — legacy rows may
  already share a canonical URL, so a `UNIQUE` would abort the backfill and raise `IntegrityError`
  on Telegram/admin ingestion. Cross-row dedup is enforced in the `submit_feed_link` service.
  Known ceiling: a race between two first-submits can create two rows.
- **Validated**: full suite 303 passed.

## Incident: deterministic line-status consolidation

- **What**: New `incident/services/line_status.py`. `consolidate(entries, scores, *, now,
  window=6h, min_reports=1)` is a pure function: a report linked to a `SocialMediaLink` is weighted
  `1 + max(net_vote, 0)`, an unlinked report `1`; the highest weight wins, ties broken by recency
  then enum severity rank; below `min_reports` it returns `None`. Message:
  `According to N social media entr{y|ies}, this line is X.` `load_line_pulses(line_ids)` is the
  batched async loader — 3 queries flat (reports, one vote aggregate, one link fetch) regardless of
  line count; a line with no recent report maps to `(status=None, message=None, count=0, links=[])`.
- **Why**: The front-page pulse must be deterministic and cheap. Amplification lets corroborated
  posts outweigh a lone tag, while a downvoted post loses influence but never disappears.
- **How**: `incident/services/line_status.py` — `ReportEntry` / `Consolidation` / `LinePulseData`
  dataclasses, `WINDOW_HOURS=6`, `MIN_REPORTS=1`, `_MAX_PULSE_LINKS=5` (`ab7af0e`).
- **Validated**: full suite 303 passed; `tests/incident/test_line_status_consolidation.py` +
  `tests/incident/test_line_status_loader.py`.

## Incident: social-link votes and the feed status filter

- **What**: `incident/services/votes.py` refactored to content-type-agnostic `_apply_vote` /
  `_remove_vote` helpers with the incident/chronology wrappers keeping their signatures, plus new
  `set_social_media_link_vote` / `remove_social_media_link_vote`. `SocialMediaLinkScalar` gained
  `normalizedUrl: String`, `voteScore: Int!` and `userVote: Int!` (batched through the existing
  `vote_scores` / `user_vote_value` loaders). `publicSocialMediaLinks` gained an optional
  `status: SocialMediaLinkStatus` argument; omitted, both LIVE and PENDING_APPROVAL are returned
  (default unchanged).
- **How**: `incident/services/votes.py`, `incident/schema/scalars.py`,
  `incident/schema/resolvers.py` (`1a9737e`, `145778b`).
- **Validated**: full suite 303 passed.

## Operation: per-line pulse fields and DataLoaders

- **What**: `operation.Line` gained `inServiceVehicleCount: Int!`, `totalVehicleCount: Int!`,
  `passengerStatus: PassengerStatus`, `passengerStatusMessage: String`, `statusReportCount: Int!`
  and `pulseLinks: [SocialMediaLinkScalar!]!`. Two new loaders in `OperationContextLoaders`:
  `line_vehicle_counts_loader` and `line_pulse_loader` (the latter imports
  `incident.services.line_status` function-locally to keep the `operation → incident` edge out of
  module import time). The front-page `lines` query is 4 queries for any number of lines.
- **Why**: The front page renders the whole network in one query; unbatched per-line resolvers
  would fan out to N×3 queries.
- **How**: `operation/schema/scalars.py`, `operation/schema/loaders.py` (`7c813fe`).
- **Validated**: full suite 303 passed; `tests/operation/test_line_pulse_fields.py`.

## Incident: feed-link submit and line-status-report services

- **What**: New `incident/services/feed_links.py`. `submit_feed_link` canonicalizes the URL, globally
  dedups against `normalized_url` (auto-upvoting the existing row instead of inserting a duplicate),
  otherwise creates a `LIVE` link with a best-effort scraped title and an optional attached
  `LineStatusReport` per line — any logged-in user, unlike `submit_social_media_link`, which queues
  non-admins. `submit_line_status_report` creates a standalone report after checking the line
  exists. New `incident/services/page_title.py::fetch_page_title` is a best-effort `httpx` `<title>`
  scrape (3s timeout, browser UA, every failure collapses to `""`).
- **How**: `incident/services/feed_links.py`, `incident/services/page_title.py` (`f0ded00`,
  `a62ed6d`).
- **Validated**: full suite 303 passed; `tests/incident/test_feed_link_submit.py`.

## Incident: feed submit and line status report mutations

- **What**: New GraphQL mutations `submitFeedLink` (`FeedLinkInput` → `FeedLinkPayload`),
  `submitLineStatusReport` (`LineStatusReportInput` → `GenericMutationReturn`),
  `upvoteSocialMediaLink`, `downvoteSocialMediaLink` and `removeSocialMediaLinkVote` — all
  `IsLoggedIn`, all on `SocialMediaLinkMutations`. New SDL inputs `FeedLinkInput` /
  `LineStatusReportInput`, type `FeedLinkPayload`, enum `PassengerStatus`; schema snapshot
  regenerated.
- **How**: `incident/schema/mutations/interactions.py`, `incident/schema/inputs.py`,
  `incident/schema/scalars.py`, `rosak/tests/snapshots/schema.graphql` (`a9418c5`).
- **Validated**: full suite 303 passed.

## Traps discovered

- Concurrent test runs share the single `test_postgres` database and destroy each other; nginx keeps
  a stale `app` upstream IP after a container restart; and
  `test_chronology_upvote_changes_downvote` is order-dependent. All three catalogued in
  `MISTAKES.md`.
